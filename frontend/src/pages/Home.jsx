import ThirdMain from "./ThirdMain/ThirdMain";

/* The home page now uses the new "Rassvet 2.0" design
   (Skills/Design2.md). The implementation lives in ThirdMain — kept
   there as the reference implementation the design doc points to —
   and is rendered here at /main. */
function Home() {
  return <ThirdMain />;
}

export default Home;
