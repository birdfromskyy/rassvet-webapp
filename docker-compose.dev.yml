# Development overrides — exposes ports for local access.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
services:
  postgres:
    ports:
      - '5433:5432'

  backend:
    ports:
      - '8080:8080'

  frontend:
    ports:
      - '3000:80'

  redis:
    ports:
      - '6379:6379'
